<resources>
    <!-- Base application theme. -->
    <style name="Theme.Meowney" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_700</item>
        <item name="colorPrimaryVariant">@color/blue_selected</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Icon color. -->
        <item name="iconTint">@color/white</item>

        <!-- Background color. -->
        <item name="android:selectableItemBackground">@color/dark_selectable</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Toolbar colors. -->
        <item name="toolbarColor">@color/black</item>
        <item name="toolbarElementColor">@color/white</item>

        <!-- Profit and deficit colors. -->
        <item name="profitColor">@color/green_profit</item>
        <item name="deficitColor">@color/red_deficit</item>
    </style>
</resources>