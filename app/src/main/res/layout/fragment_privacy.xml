<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context=".ui.more.PrivacyFragment">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/privacyToolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/toolbarColor"
        app:navigationIcon="@drawable/ic_arrow_left_black_24dp"
        app:navigationIconTint="?attr/toolbarElementColor"
        app:title="@string/title_privacy"
        app:titleTextColor="?attr/toolbarElementColor"
        app:titleCentered="true" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginLeft="24dp"
        android:layout_marginRight="24dp"
        android:background="?android:attr/listDivider"
        android:backgroundTint="?attr/toolbarElementColor" />

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/privacySwitch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:paddingEnd="30dp"
        android:text="@string/privacy_hide_recents"
        android:textSize="18sp" />

</LinearLayout>